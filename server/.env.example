# SGCS PostgreSQL Database Credentials Configuration
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=sgcs_db

# Optional Connection String (overrides individual credentials if provided)
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/sgcs_db

# Express Server Port
PORT=5000
